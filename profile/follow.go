package profile

/*
func (s *Service) Follow(follower, followee *app.Profile) (*app.Profile, error) {
	err := s.store.FollowProfile(follower, followee)
	if err != nil {
		return nil, app.InternalError(errors.Wrap(err, "failed to follow profile"))
	}

	return s.Get(followee.Name)
}
*/
